import type {Metadata} from "next";import "./globals.css";export const metadata:Metadata={title:"A3 Group SG | Premium Limousine",description:"Singapore private chauffeur, airport transfer and SG-JB transport."};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}